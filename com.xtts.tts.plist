<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.xtts.tts</string>

    <key>ProgramArguments</key>
    <array>
        <string>__VENV_PYTHON__</string>
        <string>__XTTS_DIR__/app.py</string>
    </array>

    <key>WorkingDirectory</key>
    <string>__XTTS_DIR__</string>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PYTORCH_ENABLE_MPS_FALLBACK</key>
        <string>1</string>
        <!-- Coqui XTTS ToS auto accept (matches what Pinokio does) -->
        <key>COQUI_TOS_AGREED</key>
        <string>1</string>
        <!-- Without this, Python prints only hit the log when the buffer fills -->
        <key>PYTHONUNBUFFERED</key>
        <string>1</string>
        <key>PATH</key>
        <string>/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
    </dict>

    <key>RunAtLoad</key>
    <true/>

    <!-- Restart on crash, but respect a clean exit so launchctl unload actually stops it -->
    <key>KeepAlive</key>
    <dict>
        <key>SuccessfulExit</key>
        <false/>
        <key>Crashed</key>
        <true/>
    </dict>

    <!-- 10s between restarts so it doesn't spin in a tight loop if something is broken -->
    <key>ThrottleInterval</key>
    <integer>10</integer>

    <key>StandardOutPath</key>
    <string>__LOG_DIR__/xtts.out.log</string>

    <key>StandardErrorPath</key>
    <string>__LOG_DIR__/xtts.err.log</string>

    <!-- Interactive: right priority for UI / low latency services -->
    <key>ProcessType</key>
    <string>Interactive</string>
</dict>
</plist>
